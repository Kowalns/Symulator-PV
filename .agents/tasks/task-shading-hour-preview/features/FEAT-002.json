{
  "id": "FEAT-002",
  "type": "chore",
  "description": "Add unit tests for the new /api/shading/single-hour endpoint and update SESSION-LOG.md.",
  "status": "pending",
  "steps": [
    "1. Create backend/tests/test_shading_single_hour.py with tests covering: (a) Valid request returns 200 with expected JSON structure (pozycja_slonca, panele, podsumowanie). (b) Missing body returns 400. (c) Invalid JSON returns 400. (d) Missing required fields (data, godzina, instalacja) returns 400. (e) Night hour (e.g., godzina=2 in winter) returns all zeros and noc=true. (f) Valid daytime request returns positive production values and correct panel count. Use unittest.mock.patch for pobierz_dane_tmy if needed to avoid network calls.",
    "2. Update .kiro/backups/SESSION-LOG.md with a new chronology entry describing the addition of the shading hour preview feature (endpoint + frontend visualization)."
  ],
  "acceptance_criteria": [
    "All new tests in test_shading_single_hour.py pass",
    "All existing 386 tests continue to pass (total 386 + new tests)",
    "SESSION-LOG.md updated with new entry"
  ],
  "verification": [
    "python3 -m unittest discover -s backend/tests -p 'test_*.py'"
  ],
  "blocked_reason": null,
  "findings": ""
}
